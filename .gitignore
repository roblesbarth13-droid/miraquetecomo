node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Android SDK (downloaded locally, not part of source)
android-sdk/

# Android keystore — never commit signing keys
*.jks
*.keystore
android/keystore.properties